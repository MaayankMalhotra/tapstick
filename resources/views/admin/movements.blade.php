<div class="table-wrap"><table><thead><tr><th>When (UTC)</th><th>Product</th><th>Change</th><th>Stock after</th><th>Reason</th><th>Recorded by</th></tr></thead><tbody>@forelse($movements as $movement)<tr><td>{{ $movement->created_at->utc()->format('d M Y H:i') }}</td><td>{{ $movement->product->name }}</td><td><span class="badge {{ $movement->quantity_change < 0 ? 'warn' : '' }}">{{ $movement->quantity_change > 0 ? '+' : '' }}{{ $movement->quantity_change }}</span></td><td>{{ $movement->stock_after }}</td><td>{{ $movement->reason }}</td><td>{{ $movement->user?->name ?: 'Store checkout' }}</td></tr>@empty<tr><td colspan="6" class="empty">No stock changes recorded yet.</td></tr>@endforelse</tbody></table></div>
